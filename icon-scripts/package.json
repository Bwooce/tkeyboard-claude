{
  "name": "tkeyboard-claude-bridge",
  "version": "1.0.0",
  "description": "Bridge server for T-Keyboard-S3 Claude Code integration",
  "main": "agent-bridge.js",
  "scripts": {
    "start": "node agent-bridge.js",
    "start:debug": "DEBUG=true node agent-bridge.js",
    "generate-images": "node generate-images.js",
    "generate-emoji": "node generate-images.js --emoji",
    "generate-text": "node generate-images.js --text",
    "test": "curl -s http://localhost:8081/status | jq ."
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.80",
    "bonjour-service": "^1.2.1",
    "gifencoder": "^2.0.1",
    "ws": "^8.14.0"
  },
  "devDependencies": {
    "sharp": "^0.33.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "author": "",
  "license": "MIT"
}
